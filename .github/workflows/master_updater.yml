name: Master branch updater.

on:
  release:
    types: released

jobs:
  User-agent:
    runs-on: ubuntu-latest
    steps:
      - uses: desktop-app/action_code_updater@master
        with:
          type: "dev-to-master"
