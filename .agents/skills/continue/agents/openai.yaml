interface:
  display_name: "Continue AI Work"
  short_description: "Resume unfinished work and drain the AI queue"
  default_prompt: "Use $continue to resume unfinished work, drain ready tasks, and process the inbox only when the run starts otherwise idle."
